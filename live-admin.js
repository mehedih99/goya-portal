(() => {
  const cfg=window.GOYA_CONFIG||{};
  let sb=null,liveState={dashboard:null,history:[],legacyHistory:[],controlReviews:[],rules:{},settingsLocations:[],serverOffset:0,timer:null},dashboardFilter='',controlFilter='',controlTarget=null,editTarget=null,historyMarks=new Set();
  const $=id=>document.getElementById(id),pad=n=>String(n).padStart(2,'0'),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const ctx=()=>window.GoyaAttendance?._liveContext?.()||{};
  async function client(){if(!cfg.SUPABASE_URL||!cfg.SUPABASE_ANON_KEY)throw new Error('Supabase config missing');if(!sb)sb=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY,{auth:{persistSession:false,autoRefreshToken:false}});return sb}
  async function rpc(name,args={}){const c=ctx();if(!c.token)throw new Error('Admin session expired. Please login again.');await client();const {data,error}=await sb.rpc(name,args);if(error)throw new Error(error.message);return data}
  function toast(msg,err=false){const host=$('toastHost')||document.body,d=document.createElement('div');d.className='toast'+(err?' err':'');d.textContent=msg;host.appendChild(d);setTimeout(()=>d.remove(),3800)}
  const fmtTime=v=>{if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleTimeString('en-AE',{hour:'numeric',minute:'2-digit'})};
  const fmtDate=v=>{const d=new Date(v+'T12:00:00');return d.toLocaleDateString('en-AE',{day:'2-digit',month:'short',year:'numeric'})};
  const mins=m=>{m=Math.max(0,Math.floor(Number(m)||0));return `${Math.floor(m/60)}h ${pad(m%60)}m`};
  const otText=m=>{m=Math.floor(Math.max(0,Number(m)||0)/30)*30;if(!m)return '0';if(m===30)return '30 min';const h=m/60;return `${Number.isInteger(h)?h:h.toFixed(1)} ${h===1?'hour':'hours'}`};
  function calc(row,serverNow=null){if(row?.legacy_precalc){const x=row.legacy_precalc;return {sec:Number(x.sec)||0,net:Number(x.net)||0,ot:Number(x.ot)||0,target:Number(x.target)||0,remaining:0,liveOt:Number(x.ot)||0}}const rules=liveState.rules||{},sessions=Array.isArray(row.sessions)?row.sessions:[];let sec=0;const now=serverNow?new Date(serverNow).getTime():Date.now()+liveState.serverOffset;sessions.forEach((session,index)=>{if(!session?.check_in_at)return;let start=new Date(session.check_in_at).getTime();if(index===0&&row.scheduled_start){const scheduled=new Date(`${row.operational_date}T${String(row.scheduled_start).slice(0,5)}:00+04:00`).getTime();if(Number.isFinite(scheduled)&&start<scheduled)start=scheduled}const end=session.check_out_at?new Date(session.check_out_at).getTime():now;if(Number.isFinite(start)&&Number.isFinite(end)&&end>start)sec+=(end-start)/1000});const regular=Math.max(1,Number(rules.regular_net_minutes)||480),breakMinutes=Math.max(0,Number(rules.break_minutes)||60),breakTaken=row.break_taken!==false,target=breakTaken?regular+breakMinutes:regular,presenceMinutes=sec/60,net=Math.max(0,presenceMinutes-(breakTaken?breakMinutes:0)),rawOt=Math.max(0,net-regular),ot=Math.floor(rawOt/30)*30,remainingSeconds=Math.max(0,target*60-sec),liveOtSeconds=Math.max(0,sec-target*60);return {sec,net,ot,target,remaining:remainingSeconds/60,remainingSeconds,liveOt:liveOtSeconds/60,liveOtSeconds}}
  function groupDashboard(d){const dayMap=new Map((d.days||[]).map(x=>[x.staff_id,x])),sessMap=new Map();for(const s of d.sessions||[]){if(!sessMap.has(s.staff_id))sessMap.set(s.staff_id,[]);sessMap.get(s.staff_id).push(s)}return (d.staff||[]).map(st=>{const day=dayMap.get(st.id)||{operational_date:d.operational_date,break_taken:true,classification:'Day Off - Pending Review',late_minutes:0},sessions=sessMap.get(st.id)||[];return {...day,staff_id:st.id,staff_name:st.name,sections:st.sections||[],sessions}})}
  function status(row){const ss=row.sessions||[],open=ss.some(s=>!s.check_out_at);if(open)return 'On Duty';if(ss.length){const c=row.classification||'';return c&&c!=='Duty'&&c!=='Day Off - Pending Review'?c:'Completed'}return row.classification||'Day Off - Pending Review'}
  function verification(row){if(row?.legacy)return 'Previous Record';const ss=row.sessions||[];if(!ss.length)return '—';const okIn=ss.every(x=>x.check_in_verified!==false),completed=ss.filter(x=>x.check_out_at),okOut=completed.every(x=>x.check_out_verified!==false);return okIn&&okOut?'Verified':'Review'}
  function verificationBadge(r){const v=verification(r);if(v==='Verified')return '<span class="live-verify ok">✓ Verified</span>';if(v==='Previous Record')return '<span class="live-verify neutral">Previous Record</span>';if(v==='—')return '—';return '<span class="live-verify bad">! Review</span>'}
  function renderDashboard(){const d=liveState.dashboard;if(!d)return;const rows=groupDashboard(d);let on=0,done=0,late=0,review=0;for(const r of rows){const st=status(r);if(st==='On Duty')on++;if((r.sessions||[]).length&&!r.sessions.some(x=>!x.check_out_at))done++;if(Number(r.late_minutes||0)>0)late++;if(st==='Day Off - Pending Review'||verification(r)==='Review')review++}if($('liveDOn'))$('liveDOn').textContent=on;if($('liveDCompleted'))$('liveDCompleted').textContent=done;if($('liveDLate'))$('liveDLate').textContent=late;if($('liveDPending'))$('liveDPending').textContent=review;
    const host=$('liveDashboardRows'),mobile=$('liveDashboardMobileList');const meta=[];const desktop=rows.map(r=>{const c=calc(r),ss=r.sessions||[],first=ss[0],open=ss.some(x=>!x.check_out_at),st=status(r),displaySt=open?'On Duty':(ss.length?'Completed':st),reviewFlag=st==='Day Off - Pending Review'||verification(r)==='Review';meta.push({r,c,ss,first,open,displaySt,reviewFlag});return `<tr data-status="${esc(displaySt)}" data-late="${Number(r.late_minutes)||0}" data-review="${reviewFlag?'1':'0'}"><td><strong>${esc(r.staff_name)}</strong>${open?'<span class="live-dot-label"><i></i> LIVE</span>':''}</td><td>${esc(displaySt)}</td><td>${fmtTime(first?.check_in_at)}</td><td>${mins(c.sec/60)}</td><td>${open?(c.remaining>0?mins(c.remaining):`OT ${mins(c.liveOt)}`):'—'}</td><td>${r.late_minutes?`${r.late_minutes} min`:'—'}</td><td>${ss.length}</td><td>${verificationBadge(r)}</td><td>${ss.length?`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.editDay('${r.staff_id}','${r.operational_date}')">${open?'Fix Checkout':'Edit'}</button>`:'—'}</td></tr>`}).join('');if(host)host.innerHTML=desktop;if(mobile)mobile.innerHTML=meta.map(x=>`<article class="live-dashboard-mobile-row" data-status="${esc(x.displaySt)}" data-late="${Number(x.r.late_minutes)||0}" data-review="${x.reviewFlag?'1':'0'}"><header><strong>${x.open?'<i class="live-pulse-dot"></i>':''}${esc(x.r.staff_name)}</strong><em>${esc(x.displaySt)}</em></header><div><span>Check In<b>${fmtTime(x.first?.check_in_at)}</b></span><span>Working<b>${mins(x.c.sec/60)}</b></span><span>${x.open?(x.c.remaining>0?'Remaining':'OT'):'Sessions'}<b>${x.open?(x.c.remaining>0?mins(x.c.remaining):mins(x.c.liveOt)):x.ss.length}</b></span><span>Late<b>${x.r.late_minutes?`${x.r.late_minutes}m`:'—'}</b></span></div>${x.ss.length?`<footer>${verificationBadge(x.r)}<button class="btn secondary tiny" onclick="GoyaLiveAdmin.editDay('${x.r.staff_id}','${x.r.operational_date}')">${x.open?'Fix Checkout':'Edit'}</button></footer>`:''}</article>`).join('');applyDashboardFilter()}
  async function loadDashboard(){try{const date=$('liveDashDate')?.value||new Date().toISOString().slice(0,10),d=await rpc('attendance_live_admin_dashboard',{p_token:ctx().token,p_date:date});liveState.dashboard=d;liveState.rules=d.rules||{};liveState.serverOffset=new Date(d.server_now).getTime()-Date.now();renderDashboard();applyDashboardFilter();clearInterval(liveState.timer);liveState.timer=setInterval(renderDashboard,1000)}catch(e){toast(e.message,true)}}
  function classificationOptions(cur){return ['Day Off - Pending Review','Weekly Off','Leave','Sick Leave','Unpaid Leave','Absent','Public Holiday','Other Approved','Duty'].map(x=>`<option${x===cur?' selected':''}>${x}</option>`).join('')}
  function applyMonth(){const m=$('liveHistMonth')?.value;if(!m)return;const [y,mo]=m.split('-').map(Number),end=new Date(y,mo,0).getDate();$('liveHistFrom').value=`${m}-01`;$('liveHistTo').value=`${m}-${pad(end)}`}
  function legacyMinutes(a,b){if(!a||!b)return 0;const x=new Date(a).getTime(),y=new Date(b).getTime();return Number.isFinite(x)&&Number.isFinite(y)&&y>x?(y-x)/60000:0}
  function normalizeLegacyAdmin(payload){return (payload?.rows||[]).map(x=>{const f=x.record||null,r=f||x.submission;if(!r)return null;const off=!!r.day_off,sessions=[];if(!off&&r.punch_in){sessions.push({session_no:1,check_in_at:r.punch_in,check_out_at:r.punch_out||null,check_in_verified:true,check_out_verified:!!r.punch_out,legacy:true});if(r.split_shift&&r.shift2_in)sessions.push({session_no:2,check_in_at:r.shift2_in,check_out_at:r.shift2_out||null,check_in_verified:true,check_out_verified:!!r.shift2_out,legacy:true})}const worked=sessions.reduce((a,s)=>a+legacyMinutes(s.check_in_at,s.check_out_at),0),net=f?.net_work_minutes!=null?Number(f.net_work_minutes):Math.max(0,worked-(r.break_taken===false?0:60)),ot=f?.additional_minutes!=null?Math.floor(Math.max(0,Number(f.additional_minutes))/30)*30:Math.floor(Math.max(0,net-480)/30)*30;return {operational_date:x.operational_date,staff_id:x.staff_id,staff_name:x.staff_name,break_taken:r.break_taken!==false,classification:off?'Day Off':'Duty',classification_note:r.note||null,late_minutes:Number(f?.late_minutes||r.late_minutes||0),sessions,legacy:true,legacy_precalc:{sec:worked*60,net,ot,target:r.break_taken===false?480:540}}}).filter(Boolean)}
  function mergeAdminHistory(live,legacy){const key=r=>`${r.staff_id}|${r.operational_date}`,lm=new Map(legacy.map(r=>[key(r),r])),out=[];for(const r of live){const k=key(r),has=(r.sessions||[]).length>0||(r.classification&&r.classification!=='Day Off - Pending Review');if(has){out.push(r);lm.delete(k)}else if(lm.has(k)){out.push(lm.get(k));lm.delete(k)}else out.push(r)}for(const r of lm.values())out.push(r);return out.sort((a,b)=>String(b.operational_date).localeCompare(String(a.operational_date))||String(a.staff_name).localeCompare(String(b.staff_name)))}
  async function loadHistory(){try{const from=$('liveHistFrom').value,to=$('liveHistTo').value,staff=$('liveHistStaff').value||null;if(!from||!to)return toast('Select From and To dates.',true);const [d,l]=await Promise.all([rpc('attendance_live_admin_history',{p_token:ctx().token,p_start:from,p_end:to,p_staff_id:staff}),rpc('attendance_legacy_admin_history',{p_token:ctx().token,p_start:from,p_end:to,p_staff_id:staff}).catch(()=>({rows:[]}))]);liveState.rules=d.rules||liveState.rules;liveState.serverOffset=new Date(d.server_now).getTime()-Date.now();liveState.legacyHistory=normalizeLegacyAdmin(l);liveState.history=mergeAdminHistory(d.rows||[],liveState.legacyHistory);renderHistory()}catch(e){toast(e.message,true)}}
  function filteredRows(){const filter=$('liveHistStatus')?.value||'';return (liveState.history||[]).filter(r=>!filter||status(r)===filter||r.classification===filter)}
  function summaryFor(rows){const o={days:rows.length,duty:0,weekly:0,leave:0,sick:0,unpaid:0,absent:0,holiday:0,other:0,pending:0,work:0,net:0,ot:0,lateDays:0,lateMin:0,noBreak:0,split:0,review:0};rows.forEach(r=>{const c=calc(r),ss=r.sessions||[],st=status(r);if(ss.length){o.duty++;o.work+=c.sec/60;o.net+=c.net;o.ot+=c.ot;if(r.break_taken===false)o.noBreak++;if(ss.length>1)o.split++;if(verification(r)!=='Verified'||ss.some(s=>!s.check_out_at))o.review++}if(r.late_minutes){o.lateDays++;o.lateMin+=Number(r.late_minutes)||0}if(st==='Weekly Off')o.weekly++;else if(st==='Leave')o.leave++;else if(st==='Sick Leave')o.sick++;else if(st==='Unpaid Leave')o.unpaid++;else if(st==='Absent')o.absent++;else if(st==='Public Holiday')o.holiday++;else if(st==='Other Approved')o.other++;else if(st==='Day Off - Pending Review')o.pending++});return o}
  function renderSummary(rows){const s=summaryFor(rows),host=$('liveReportSummary');if(host)host.innerHTML=[['Duty',s.duty],['Off',s.weekly+s.pending],['Leave',s.leave+s.sick+s.unpaid],['Working',mins(s.work)],['Net',mins(s.net)],['OT',otText(s.ot)],['Late',s.lateDays],['Review',s.review]].map(([k,v])=>`<div><span>${k}</span><strong>${v}</strong></div>`).join('');const by=new Map();rows.forEach(r=>{if(!by.has(r.staff_id))by.set(r.staff_id,[]);by.get(r.staff_id).push(r)});const ph=$('livePersonSummary');if(ph)ph.innerHTML=[...by.values()].map(rs=>{const x=summaryFor(rs),r=rs[0];return `<article><strong>${esc(r.staff_name)}</strong><span>Duty <b>${x.duty}</b></span><span>Net <b>${mins(x.net)}</b></span><span>OT <b>${otText(x.ot)}</b></span><span>Late <b>${x.lateDays}</b></span></article>`}).join('')}
  function rowKey(r){return `${r.staff_id}|${r.operational_date}`}
  function isReviewRow(r){const ss=r.sessions||[];if(status(r)==='Day Off - Pending Review')return true;if(verification(r)==='Review')return true;if(r.legacy&&ss.some(s=>!s.check_in_at||!s.check_out_at))return true;return false}
  function updateMarkedCount(){const el=$('liveHistoryMarkedCount');if(el)el.textContent=`${historyMarks.size} marked`}
  function toggleHistoryMark(staffId,date,checked){const k=`${staffId}|${date}`;checked?historyMarks.add(k):historyMarks.delete(k);updateMarkedCount()}
  function toggleAllHistory(checked){for(const r of filteredRows()){const k=rowKey(r);checked?historyMarks.add(k):historyMarks.delete(k)}renderHistory()}
  function clearHistoryMarks(){historyMarks.clear();if($('liveHistoryMarkAll'))$('liveHistoryMarkAll').checked=false;renderHistory()}
  function markedRows(){const map=new Map((liveState.history||[]).map(r=>[rowKey(r),r]));return [...historyMarks].map(k=>map.get(k)).filter(Boolean)}
  async function bulkSetBreak(value){const rows=markedRows();if(!rows.length)return toast('Mark one or more records first.',true);if(!confirm(`Set Break ${value?'Yes':'No'} for ${rows.length} marked record(s)?`))return;let ok=0;for(const r of rows){try{await rpc('attendance_live_admin_set_break',{p_token:ctx().token,p_staff_id:r.staff_id,p_date:r.operational_date,p_break_taken:value});ok++}catch(e){toast(`${r.staff_name} ${r.operational_date}: ${e.message}`,true)}}toast(`Break updated for ${ok} record(s).`);historyMarks.clear();await loadHistory()}
  async function bulkEditSelected(){const rows=markedRows();if(rows.length!==1)return toast('Mark exactly one record to edit.',true);await editDay(rows[0].staff_id,rows[0].operational_date)}
  function renderHistory(){const rows=filteredRows();renderSummary(rows);const table=$('liveHistoryAdminRows');if(table)table.innerHTML=rows.length?rows.map(r=>{const c=calc(r),ss=r.sessions||[],st=status(r),isNo=!ss.length&&!r.legacy,checked=historyMarks.has(rowKey(r))?'checked':'',primary=isNo?`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.saveClassification('${r.staff_id}','${r.operational_date}',this)">Save</button>`:`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.editDay('${r.staff_id}','${r.operational_date}')">Edit</button>`,del=r.legacy?'':`<button class="btn danger tiny" onclick="GoyaLiveAdmin.deleteHistoryDay('${r.staff_id}','${r.operational_date}','${esc(r.staff_name)}')">Delete</button>`;return `<tr><td><input type="checkbox" ${checked} onchange="GoyaLiveAdmin.toggleHistoryMark('${r.staff_id}','${r.operational_date}',this.checked)"></td><td>${esc(r.operational_date)}</td><td><strong>${esc(r.staff_name)}</strong>${r.legacy?'<span class="previous-inline">Previous</span>':''}</td><td>${ss.length?ss.map(s=>`${fmtTime(s.check_in_at)} → ${fmtTime(s.check_out_at)}`).join('<br>'):'—'}</td><td>${ss.length}</td><td>${ss.length?mins(c.sec/60):'—'}</td><td>${ss.length?mins(c.net):'—'}</td><td>${c.ot?otText(c.ot):'—'}</td><td>${r.late_minutes?`${r.late_minutes} min`:'—'}</td><td>${ss.length?(r.break_taken!==false?'Yes':'No'):'—'}</td><td>${isNo?`<select class="live-classify" data-staff="${r.staff_id}" data-date="${r.operational_date}">${classificationOptions(r.classification||'Day Off - Pending Review')}</select>`:esc(st)}</td><td>${ss.length?verificationBadge(r):'—'}</td><td><div class="live-row-actions">${primary}${del}</div></td></tr>`}).join(''):'<tr><td colspan="13" class="empty">No records for selected filters.</td></tr>';const cards=$('liveHistoryMobileCards');if(cards)cards.innerHTML=rows.length?rows.map(r=>{const c=calc(r),ss=r.sessions||[],st=status(r),isNo=!ss.length&&!r.legacy,checked=historyMarks.has(rowKey(r))?'checked':'';return `<article class="live-history-admin-card"><header><div><label class="mobile-mark"><input type="checkbox" ${checked} onchange="GoyaLiveAdmin.toggleHistoryMark('${r.staff_id}','${r.operational_date}',this.checked)"> Mark</label><b>${esc(r.staff_name)}</b><span>${fmtDate(r.operational_date)}${r.legacy?' · Previous':''}</span></div><em>${esc(st)}</em></header>${ss.length?`<div class="mobile-session-line">${ss.map(x=>`<span>S${x.session_no}: ${fmtTime(x.check_in_at)} → ${fmtTime(x.check_out_at)}</span>`).join('')}</div>`:''}<div class="mobile-metric-grid"><span>Worked<b>${ss.length?mins(c.sec/60):'—'}</b></span><span>Net<b>${ss.length?mins(c.net):'—'}</b></span><span>OT<b>${c.ot?otText(c.ot):'—'}</b></span><span>Late<b>${r.late_minutes?`${r.late_minutes}m`:'—'}</b></span></div><div class="mobile-history-actions">${isNo?`<select class="live-classify" data-staff="${r.staff_id}" data-date="${r.operational_date}">${classificationOptions(r.classification||'Day Off - Pending Review')}</select><button class="btn secondary tiny" onclick="GoyaLiveAdmin.saveClassificationMobile('${r.staff_id}','${r.operational_date}',this)">Save</button>`:`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.editDay('${r.staff_id}','${r.operational_date}')">Edit</button>${r.legacy?'':`<button class="btn danger tiny" onclick="GoyaLiveAdmin.deleteHistoryDay('${r.staff_id}','${r.operational_date}','${esc(r.staff_name)}')">Delete</button>`}`}</div></article>`}).join(''):'<div class="live-empty">No records for selected filters.</div>';updateMarkedCount()}
  async function deleteHistoryDay(staffId,date,staffName){if(!confirm(`Delete ${staffName} attendance for ${date} from active history/reports?\n\nThe record will be archived in Supabase audit history and can be restored later.`))return;try{await rpc('attendance_live_admin_delete_day',{p_token:ctx().token,p_staff_id:staffId,p_date:date,p_reason:null});toast('Attendance archived and removed from active reports.');await loadHistory();if(!$('attLiveDashboard')?.classList.contains('hidden'))await loadDashboard()}catch(e){toast(e.message,true)}}
  async function saveClassification(staffId,date,btn){const sel=btn.closest('tr')?.querySelector('.live-classify');if(!sel)return;const val=sel.value,note=prompt('Optional admin note for this day:','')||null;try{await rpc('attendance_live_admin_classify_day',{p_token:ctx().token,p_staff_id:staffId,p_date:date,p_classification:val,p_note:note});toast('Day classification saved.');await loadHistory()}catch(e){toast(e.message,true)}}
  async function saveClassificationMobile(staffId,date,btn){const sel=btn.parentElement?.querySelector('.live-classify');if(!sel)return;try{await rpc('attendance_live_admin_classify_day',{p_token:ctx().token,p_staff_id:staffId,p_date:date,p_classification:sel.value,p_note:null});toast('Day classification saved.');await loadHistory()}catch(e){toast(e.message,true)}}
  function toLocalInput(v){if(!v)return '';const d=new Date(v),z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}T${z(d.getHours())}:${z(d.getMinutes())}`}
  function legacyOutInput(checkIn,checkOut){
    if(!checkOut)return '';
    let i=checkIn?new Date(checkIn):null,o=new Date(checkOut);
    if(i && !Number.isNaN(i.getTime()) && !Number.isNaN(o.getTime()) && o<=i){
      let guard=0;
      while(o<=i && guard<2){o=new Date(o.getTime()+86400000);guard++}
      const dur=(o-i)/3600000;
      if(dur>0 && dur<=24)return toLocalInput(o);
    }
    return toLocalInput(checkOut);
  }
  async function ensureDayInHistory(staffId,date){let r=(liveState.history||[]).find(x=>x.staff_id===staffId&&x.operational_date===date);if(r)return r;try{const d=await rpc('attendance_live_admin_history',{p_token:ctx().token,p_start:date,p_end:date,p_staff_id:staffId});r=(d.rows||[])[0];return r}catch{return null}}
  async function editDay(staffId,date){let r=(liveState.history||[]).find(x=>x.staff_id===staffId&&x.operational_date===date);if(!r)r=await ensureDayInHistory(staffId,date);if(!r)return toast('Attendance record not found.',true);editTarget=r;const modal=$('liveEditModal'),list=$('liveEditSessions');$('liveEditBreak').value=r.break_taken===false?'false':'true';$('liveEditBreakSave').classList.toggle('hidden',!!r.legacy);$('liveEditLegacyActions').classList.toggle('hidden',!r.legacy);const ss=r.sessions||[];if(r.legacy){list.innerHTML=[0,1].map((idx)=>{const s=ss[idx],show=idx===0||s;return show?`<div class="live-edit-session legacy-edit-session"><strong>Session ${idx+1}</strong><label>Check In<input type="datetime-local" data-legacy-field="in${idx+1}" value="${toLocalInput(s?.check_in_at)}"></label><label>Check Out<input type="datetime-local" data-legacy-field="out${idx+1}" value="${legacyOutInput(s?.check_in_at,s?.check_out_at)}"></label></div>`:''}).join('')+(!ss[1]?'<button class="btn secondary tiny" onclick="GoyaLiveAdmin.addLegacySecondSession(this)">+ Add Session 2</button>':'')}else{if(!ss.length)return toast('No live session exists for this day.',true);list.innerHTML=ss.map(s=>`<div class="live-edit-session"><strong>Session ${s.session_no}</strong><label>Check In<input type="datetime-local" data-id="${s.id}" data-field="in" value="${toLocalInput(s.check_in_at)}"></label><label>Check Out<input type="datetime-local" data-id="${s.id}" data-field="out" value="${toLocalInput(s.check_out_at)}"></label><button class="btn secondary tiny" onclick="GoyaLiveAdmin.saveSessionEdit('${s.id}',this)">${s.check_out_at?'Save Session':'Set Checkout'}</button></div>`).join('')}$('liveEditTitle').textContent=`${r.staff_name} · ${date}${r.legacy?' · Previous Record':''}`;modal.classList.remove('hidden')}
  function addLegacySecondSession(btn){if(!editTarget?.legacy)return;btn.remove();$('liveEditSessions').insertAdjacentHTML('beforeend',`<div class="live-edit-session legacy-edit-session"><strong>Session 2</strong><label>Check In<input type="datetime-local" data-legacy-field="in2"></label><label>Check Out<input type="datetime-local" data-legacy-field="out2"></label></div>`)}
  async function saveEditBreak(){if(!editTarget)return;const value=$('liveEditBreak').value==='true';try{await rpc('attendance_live_admin_set_break',{p_token:ctx().token,p_staff_id:editTarget.staff_id,p_date:editTarget.operational_date,p_break_taken:value});toast('Break updated and attendance recalculated.');editTarget.break_taken=value;closeEdit();await loadHistory();if(!$('attLiveControl')?.classList.contains('hidden'))await loadControl()}catch(e){toast(e.message,true)}}
  async function savePreviousRecordEdit(){
    if(!editTarget?.legacy)return;
    const val=n=>$(`[data-legacy-field="${n}"]`)?.value||null;
    let pin=val('in1'),pout=val('out1'),s2in=val('in2'),s2out=val('out2');
    const br=$('liveEditBreak').value==='true';
    if(!pin)return toast('Session 1 Check In is required.',true);

    const normalizePair=(inVal,outVal,label)=>{
      if(!outVal)return {inVal,outVal:null};
      let i=new Date(inVal),o=new Date(outVal),guard=0;
      if(Number.isNaN(i.getTime())||Number.isNaN(o.getTime()))throw new Error(`${label}: invalid date/time.`);
      while(o<=i && guard<2){o=new Date(o.getTime()+86400000);guard++}
      const hours=(o-i)/3600000;
      if(o<=i || hours<=0 || hours>24)throw new Error(`${label}: Check Out must be after Check In and within 24 hours.`);
      const z=n=>String(n).padStart(2,'0');
      const local=`${o.getFullYear()}-${z(o.getMonth()+1)}-${z(o.getDate())}T${z(o.getHours())}:${z(o.getMinutes())}`;
      return {inVal,outVal:local};
    };

    try{
      ({inVal:pin,outVal:pout}=normalizePair(pin,pout,'Session 1'));
      if((s2in&&!s2out)||(!s2in&&s2out))throw new Error('Session 2 requires both Check In and Check Out.');
      if(s2in)({inVal:s2in,outVal:s2out}=normalizePair(s2in,s2out,'Session 2'));

      const btn=document.querySelector('#liveEditLegacyActions .btn.primary');
      if(btn){btn.disabled=true;btn.textContent='Saving...'}
      await rpc('attendance_live_admin_update_previous_record',{
        p_token:ctx().token,
        p_staff_id:editTarget.staff_id,
        p_date:editTarget.operational_date,
        p_punch_in:localInputToIso(pin),
        p_punch_out:pout?localInputToIso(pout):null,
        p_break_taken:br,
        p_split_shift:!!s2in,
        p_shift2_in:s2in?localInputToIso(s2in):null,
        p_shift2_out:s2out?localInputToIso(s2out):null,
        p_note:editTarget.classification_note||null
      });
      toast('Previous record saved and recalculated.');
      closeEdit();
      await loadHistory();
      if(!$('attLiveControl')?.classList.contains('hidden'))await loadControl();
    }catch(e){
      toast(e.message||'Could not save previous record.',true);
    }finally{
      const btn=document.querySelector('#liveEditLegacyActions .btn.primary');
      if(btn){btn.disabled=false;btn.textContent='Save Previous Record'}
    }
  }
  function localInputToIso(v){if(!v)return null;const d=new Date(v);return d.toISOString()}
  async function saveSessionEdit(id,btn){const box=btn.closest('.live-edit-session'),pin=box.querySelector('[data-field="in"]').value,pout=box.querySelector('[data-field="out"]').value;if(!pout&&!confirm('Check Out is blank. Save this session as incomplete?'))return;try{await rpc('attendance_live_admin_update_session',{p_token:ctx().token,p_session_id:id,p_check_in:localInputToIso(pin),p_check_out:localInputToIso(pout)});toast('Session corrected. Original values remain in audit history.');closeEdit();if(!$('attLiveHistory')?.classList.contains('hidden'))await loadHistory();if(!$('attLiveDashboard')?.classList.contains('hidden'))await loadDashboard();if(!$('attLiveControl')?.classList.contains('hidden'))await loadControl()}catch(e){toast(e.message,true)}}
  function closeEdit(){$('liveEditModal')?.classList.add('hidden');editTarget=null}
  function detailRows(rows=filteredRows()){return rows.map(r=>{const c=calc(r),ss=r.sessions||[];return {'Date':r.operational_date,'Staff':r.staff_name,'Shift Sessions':ss.length,'Session Details':ss.map(s=>`Shift ${s.session_no}: ${fmtTime(s.check_in_at)} - ${fmtTime(s.check_out_at)}`).join(' | '),'Worked':ss.length?mins(c.sec/60):'','Net':ss.length?mins(c.net):'','OT':c.ot?otText(c.ot):'','Late Minutes':r.late_minutes||0,'Break Taken':ss.length?(r.break_taken!==false?'Yes':'No'):'','Status':status(r),'Verification':verification(r),'Note':r.classification_note||''}})}
  function safeName(v){return String(v||'All_Staff').replace(/[^a-z0-9]+/gi,'_').replace(/^_|_$/g,'')||'All_Staff'}
  function reportFileBase(ext){const staffSel=$('liveHistStaff'),staff=staffSel?.value?staffSel.options[staffSel.selectedIndex]?.text:'All_Staff',from=$('liveHistFrom').value,to=$('liveHistTo').value;return `Goya_Live_Attendance_${safeName(staff)}_${from}_to_${to}.${ext}`}
  function summarySheetRows(rows){const by=new Map();rows.forEach(r=>{if(!by.has(r.staff_id))by.set(r.staff_id,[]);by.get(r.staff_id).push(r)});return [...by.values()].map(rs=>{const x=summaryFor(rs),r=rs[0];return {'Staff':r.staff_name,'Duty Days':x.duty,'Weekly Off':x.weekly,'Leave':x.leave,'Sick Leave':x.sick,'Unpaid Leave':x.unpaid,'Absent':x.absent,'Public Holiday':x.holiday,'Other Approved':x.other,'Pending Review':x.pending,'Working Hours':mins(x.work),'Net Hours':mins(x.net),'OT':otText(x.ot),'Late Days':x.lateDays,'Late Minutes':x.lateMin,'No Break Days':x.noBreak,'Split Shift Days':x.split,'Verification Review':x.review}})}
  function exportExcel(){const rows=filteredRows();if(!rows.length)return toast('Load history first.',true);if(!window.XLSX)return toast('Excel library is still loading.',true);const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(summarySheetRows(rows)),'Summary');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(detailRows(rows)),'Daily Details');const by=new Map();rows.forEach(r=>{if(!by.has(r.staff_id))by.set(r.staff_id,[]);by.get(r.staff_id).push(r)});for(const rs of by.values()){let nm=safeName(rs[0].staff_name).slice(0,31)||'Staff';if(wb.SheetNames.includes(nm))nm=(nm.slice(0,27)+'_'+Math.random().toString(36).slice(2,5)).slice(0,31);XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(detailRows(rs)),nm)}XLSX.writeFile(wb,reportFileBase('xlsx'))}
  function exportPDF(){const rows=filteredRows();if(!rows.length)return toast('Load history first.',true);if(!window.jspdf?.jsPDF)return toast('PDF library is still loading.',true);const {jsPDF}=window.jspdf,doc=new jsPDF({orientation:'landscape'}),from=$('liveHistFrom').value,to=$('liveHistTo').value,by=new Map();rows.forEach(r=>{if(!by.has(r.staff_id))by.set(r.staff_id,[]);by.get(r.staff_id).push(r)});let first=true;for(const rs of by.values()){if(!first)doc.addPage();first=false;const r0=rs[0],x=summaryFor(rs);doc.setFontSize(18);doc.text('GOYA - Live Attendance Report',14,15);doc.setFontSize(12);doc.text(r0.staff_name,14,23);doc.setFontSize(9);doc.text(`Period: ${from} to ${to}`,14,29);doc.text(`Duty: ${x.duty}   Weekly Off: ${x.weekly}   Leave: ${x.leave+x.sick+x.unpaid}   Absent: ${x.absent}   Working: ${mins(x.work)}   Net: ${mins(x.net)}   OT: ${otText(x.ot)}   Late: ${x.lateDays} day(s) / ${x.lateMin} min   Split: ${x.split}`,14,35);doc.autoTable({startY:40,head:[['Date','Sessions','Working Time','Worked','Net','OT','Late','Break','Status','Verification']],body:rs.map(r=>{const c=calc(r),ss=r.sessions||[];return [r.operational_date,ss.length,ss.length?ss.map(x=>`${fmtTime(x.check_in_at)}-${fmtTime(x.check_out_at)}`).join(' | '):'—',ss.length?mins(c.sec/60):'—',ss.length?mins(c.net):'—',c.ot?otText(c.ot):'—',r.late_minutes?`${r.late_minutes} min`:'—',ss.length?(r.break_taken!==false?'Yes':'No'):'—',status(r),verification(r)]}),styles:{fontSize:7.5,cellPadding:2},headStyles:{fillColor:[36,57,49]}})}doc.save(reportFileBase('pdf'))}
  function overtimeSummaryRows(){const by=new Map();for(const r of filteredRows()){const c=calc(r);if(c.ot<=0)continue;if(!by.has(r.staff_id))by.set(r.staff_id,{staff:r.staff_name,ot:0,days:0});const x=by.get(r.staff_id);x.ot+=c.ot;x.days++}return [...by.values()].sort((a,b)=>b.ot-a.ot)}
  function overtimeFileBase(ext){const staffSel=$('liveHistStaff'),staff=staffSel?.value?staffSel.options[staffSel.selectedIndex]?.text:'All_Staff',from=$('liveHistFrom').value,to=$('liveHistTo').value;return `Goya_Overtime_Only_${safeName(staff)}_${from}_to_${to}.${ext}`}
  function exportOvertimeExcel(){const data=overtimeSummaryRows();if(!data.length)return toast('No overtime found for the selected period.',true);if(!window.XLSX)return toast('Excel library is still loading.',true);const rows=data.map(x=>({'Staff':x.staff,'OT Days':x.days,'Total OT':otText(x.ot),'OT Minutes':x.ot})),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows),'Overtime Summary');XLSX.writeFile(wb,overtimeFileBase('xlsx'))}
  function exportOvertimePDF(){const data=overtimeSummaryRows();if(!data.length)return toast('No overtime found for the selected period.',true);if(!window.jspdf?.jsPDF)return toast('PDF library is still loading.',true);const {jsPDF}=window.jspdf,doc=new jsPDF({orientation:'portrait'}),from=$('liveHistFrom').value,to=$('liveHistTo').value,total=data.reduce((a,x)=>a+x.ot,0);doc.setFontSize(18);doc.text('GOYA - Overtime Summary',14,16);doc.setFontSize(10);doc.text(`Period: ${from} to ${to}`,14,24);doc.text(`Staff with OT: ${data.length}   Total OT: ${otText(total)}`,14,30);doc.autoTable({startY:36,head:[['Staff','OT Days','Total OT']],body:data.map(x=>[x.staff,x.days,otText(x.ot)]),styles:{fontSize:9,cellPadding:2.5},headStyles:{fillColor:[36,57,49]},margin:{left:14,right:14}});doc.save(overtimeFileBase('pdf'))}
  async function getPublicIp(){for(const url of ['https://api.ipify.org?format=json','https://api64.ipify.org?format=json']){try{const r=await fetch(url,{cache:'no-store'});if(r.ok){const j=await r.json();if(j.ip)return j.ip}}catch{}}throw new Error('Could not detect public IP.')}
  function getGps(){return new Promise((resolve,reject)=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy}),e=>reject(new Error(e.code===1?'Allow location permission first.':'Could not get GPS.')),{enableHighAccuracy:true,timeout:15000}):reject(new Error('GPS unsupported.')))}
  function renderLocations(){const host=$('liveLocationList');if(!host)return;host.innerHTML=liveState.settingsLocations.length?liveState.settingsLocations.map((l,i)=>`<div class="live-location-row"><div><strong>${esc(l.name||'Workplace')}</strong><span>${Number(l.lat).toFixed(6)}, ${Number(l.lon).toFixed(6)} · ${l.radius_m||150}m</span></div><button class="btn danger tiny" onclick="GoyaLiveAdmin.removeLocation(${i})">Remove</button></div>`).join(''):'<div class="live-empty">No workplace location configured yet.</div>'}
  function updateLiveRuleState(){const split=$('liveSplitShiftEnabled')?.checked!==false,br=$('liveAllowBreakSelection')?.checked!==false;if($('liveSplitRuleState'))$('liveSplitRuleState').textContent=`Split Shift: ${split?'Enabled':'Disabled'}`;if($('liveBreakRuleState'))$('liveBreakRuleState').textContent=`Break Selection: ${br?'Enabled':'Disabled'}`}
  async function loadSettings(){try{const r=await rpc('attendance_live_admin_settings',{p_token:ctx().token})||{};liveState.rules=r;liveState.settingsLocations=Array.isArray(r.locations)?r.locations:[];if($('liveRequireLocation'))$('liveRequireLocation').checked=r.require_location!==false;if($('liveRequireNetwork'))$('liveRequireNetwork').checked=r.require_network!==false;if($('liveSplitShiftEnabled'))$('liveSplitShiftEnabled').checked=r.split_shift_enabled!==false;if($('liveAllowBreakSelection'))$('liveAllowBreakSelection').checked=r.allow_break_selection!==false;if($('liveDefaultBreakTaken'))$('liveDefaultBreakTaken').value=r.default_break_taken===false?'false':'true';if($('liveDutyHours'))$('liveDutyHours').value=(Number(r.duty_presence_minutes)||540)/60;if($('liveBreakMinutes'))$('liveBreakMinutes').value=Number(r.break_minutes)||60;if($('liveApprovedIps'))$('liveApprovedIps').value=(Array.isArray(r.approved_public_ips)?r.approved_public_ips:[]).join('\n');renderLocations();updateLiveRuleState();$('liveSplitShiftEnabled')?.addEventListener('change',updateLiveRuleState,{once:false});$('liveAllowBreakSelection')?.addEventListener('change',updateLiveRuleState,{once:false})}catch(e){toast(e.message,true)}}
  async function detectLocation(){try{const g=await getGps();$('liveLocLat').value=g.lat.toFixed(7);$('liveLocLon').value=g.lon.toFixed(7);toast(`GPS captured ±${Math.round(g.accuracy||0)}m`)}catch(e){toast(e.message,true)}}
  async function detectIp(){try{const ip=await getPublicIp(),box=$('liveApprovedIps'),ips=box.value.split(/\s+/).filter(Boolean);if(!ips.includes(ip))ips.push(ip);box.value=ips.join('\n');toast(`Current public IP added: ${ip}`)}catch(e){toast(e.message,true)}}
  function addLocation(){const name=$('liveLocName').value.trim()||'Workplace',lat=+$('liveLocLat').value,lon=+$('liveLocLon').value,radius=+$('liveLocRadius').value||150;if(!Number.isFinite(lat)||!Number.isFinite(lon))return toast('Capture or enter valid GPS coordinates.',true);liveState.settingsLocations.push({name,lat,lon,radius_m:radius,active:true});$('liveLocName').value='';renderLocations()}
  function removeLocation(i){liveState.settingsLocations.splice(i,1);renderLocations()}
  async function saveSettings(){const ips=[...new Set($('liveApprovedIps').value.split(/\s+/).map(x=>x.trim()).filter(Boolean))],value={require_location:$('liveRequireLocation').checked,require_network:$('liveRequireNetwork').checked,split_shift_enabled:$('liveSplitShiftEnabled')?$('liveSplitShiftEnabled').checked:true,allow_break_selection:$('liveAllowBreakSelection')?$('liveAllowBreakSelection').checked:true,default_break_taken:$('liveDefaultBreakTaken')?$('liveDefaultBreakTaken').value!=='false':true,duty_presence_minutes:Math.round((+$('liveDutyHours').value||9)*60),regular_net_minutes:480,break_minutes:+$('liveBreakMinutes').value||60,late_grace_minutes:15,operational_cutoff:'02:00',shift_start_times:['08:00','09:00','10:00','11:00','12:00','13:00','14:00','15:00','16:00','17:00'],locations:liveState.settingsLocations,approved_public_ips:ips};try{liveState.rules=await rpc('attendance_live_admin_save_settings',{p_token:ctx().token,p_value:value});updateLiveRuleState();toast('Live Attendance settings saved. Staff My Attendance will use the new Split Shift/Break rules after refresh.')}catch(e){toast(e.message,true)}}
  function fillStaffFilter(){const sel=$('liveHistStaff'),staff=ctx().staff||[];if(sel&&!sel.dataset.ready){sel.innerHTML='<option value="">All Staff</option>'+staff.map(s=>`<option value="${s.portal_staff_id||s.id}">${esc(s.name)}</option>`).join('');sel.dataset.ready='1'}}

  function setDashboardFilter(filter){dashboardFilter=filter||'';document.querySelectorAll('.live-stat-btn').forEach(x=>x.classList.toggle('active-filter',!!dashboardFilter&&x.dataset.filter===dashboardFilter));const label=$('liveDashboardFilterLabel');if(label)label.textContent=dashboardFilter?`Filtered: ${dashboardFilter}`:'Showing all staff';applyDashboardFilter()}
  function applyDashboardFilter(){const match=el=>{if(!dashboardFilter)return true;if(dashboardFilter==='Late')return Number(el.dataset.late||0)>0;if(dashboardFilter==='Review')return el.dataset.review==='1';if(dashboardFilter==='Completed')return el.dataset.status==='Completed';return el.dataset.status===dashboardFilter};document.querySelectorAll('#liveDashboardRows tr,#liveDashboardMobileList .live-dashboard-mobile-row').forEach(r=>r.hidden=!match(r))}
  async function setControlFilter(filter){controlFilter=filter||'';document.querySelectorAll('.live-control-stat').forEach(x=>x.classList.toggle('active-filter',!!controlFilter&&x.dataset.filter===controlFilter));if(controlFilter==='Review'&&!liveState.controlReviews.length)await loadControlReviewQueue();renderControl()}
  async function loadControlReviewQueue(){try{const to=$('liveControlDate')?.value||new Date().toISOString().slice(0,10),year=String(to).slice(0,4),staff=$('liveControlStaff')?.value||null,[d,l]=await Promise.all([rpc('attendance_live_admin_history',{p_token:ctx().token,p_start:`${year}-01-01`,p_end:to,p_staff_id:staff}),rpc('attendance_legacy_admin_history',{p_token:ctx().token,p_start:`${year}-01-01`,p_end:to,p_staff_id:staff}).catch(()=>({rows:[]}))]);const merged=mergeAdminHistory(d.rows||[],normalizeLegacyAdmin(l));liveState.controlReviews=merged.filter(isReviewRow)}catch(e){toast(e.message,true);liveState.controlReviews=[]}}
  function renderControl(){const d=liveState.dashboard;if(!d)return;const base=groupDashboard(d),staffFilter=$('liveControlStaff')?.value||'',reviews=liveState.controlReviews||[];let on=0,done=0;for(const r of base){const st=status(r);if(st==='On Duty')on++;if((r.sessions||[]).length&&!r.sessions.some(x=>!x.check_out_at))done++}const reviewCount=reviews.length;const sum=$('liveControlSummary');if(sum)sum.innerHTML=[['On Duty',on,'On Duty'],['Shift Done',done,'Completed'],['Review',reviewCount,'Review']].map(([k,v,f])=>`<button class="live-control-stat ${controlFilter===f?'active-filter':''}" data-filter="${f}" onclick="GoyaLiveAdmin.setControlFilter('${f}')"><span>${k}</span><strong>${v}</strong></button>`).join('')+`<button class="live-control-clear" onclick="GoyaLiveAdmin.setControlFilter('')">All</button>`;let list=controlFilter==='Review'?reviews:base;if(staffFilter)list=list.filter(r=>r.staff_id===staffFilter);if(controlFilter&&controlFilter!=='Review'){list=list.filter(r=>{const st=status(r);if(controlFilter==='On Duty')return st==='On Duty';if(controlFilter==='Completed')return (r.sessions||[]).length&&!r.sessions.some(x=>!x.check_out_at);return true})}const note=$('liveControlModeNote');if(note){note.classList.toggle('hidden',controlFilter!=='Review');note.textContent=controlFilter==='Review'?'Review mode shows unresolved records from earlier dates too. Fix a record and it will leave this queue when resolved.':''}const host=$('liveControlList');if(host)host.innerHTML=list.length?list.map(r=>{const c=calc(r),ss=r.sessions||[],open=ss.some(x=>!x.check_out_at),st=status(r),displaySt=open?'On Duty':(ss.length?'Shift Done':st),first=ss[0],dateTag=controlFilter==='Review'?`<span class="control-date">${esc(r.operational_date)}</span>`:'';return `<div class="live-control-row ${open?'on-duty':''}"><div class="control-staff"><strong>${open?'<i class="live-pulse-dot"></i>':''}${esc(r.staff_name)}</strong><span>${dateTag}${esc(displaySt)}${r.legacy?' · Previous':''}</span></div><div><small>Check In</small><b>${fmtTime(first?.check_in_at)}</b></div><div><small>Working</small><b>${mins(c.sec/60)}</b></div><div><small>${open?(c.remaining>0?'Remaining':'OT'):'Sessions'}</small><b>${open?(c.remaining>0?mins(c.remaining):mins(c.liveOt)):ss.length}</b></div><div><small>Late</small><b>${r.late_minutes?`${r.late_minutes}m`:'—'}</b></div><div class="control-actions">${controlFilter==='Review'?`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.openHistoryForRecord('${r.staff_id}','${r.operational_date}')">Open / Fix</button>`:`<button class="btn success tiny" ${open?'disabled':''} onclick="GoyaLiveAdmin.openManualControl('${r.staff_id}','${esc(r.staff_name)}','${ss.length?'REJOIN':'CHECK_IN'}')">${ss.length?'Rejoin':'Check In'}</button><button class="btn danger tiny" ${open?'':'disabled'} onclick="GoyaLiveAdmin.openManualControl('${r.staff_id}','${esc(r.staff_name)}','CHECK_OUT')">Check Out</button>${ss.length?`<button class="btn secondary tiny" onclick="GoyaLiveAdmin.editDay('${r.staff_id}','${r.operational_date}')">Edit</button>`:''}`}</div></div>`}).join(''):'<div class="live-empty">No staff in this filter.</div>'}
  async function openHistoryForRecord(staffId,date){GoyaAttendance.nav(document.querySelector('[onclick*="attLiveHistory"]'),'attLiveHistory');await openHistory();$('liveHistFrom').value=date;$('liveHistTo').value=date;$('liveHistStaff').value=staffId;await loadHistory();setTimeout(()=>editDay(staffId,date),50)}
  async function loadControl(){try{const date=$('liveControlDate')?.value||new Date().toISOString().slice(0,10),d=await rpc('attendance_live_admin_dashboard',{p_token:ctx().token,p_date:date});liveState.dashboard=d;liveState.rules=d.rules||{};liveState.serverOffset=new Date(d.server_now).getTime()-Date.now();const sel=$('liveControlStaff');if(sel){const cur=sel.value;sel.innerHTML='<option value="">All Staff</option>'+(d.staff||[]).map(s=>`<option value="${s.id}">${esc(s.name)}</option>`).join('');if([...sel.options].some(o=>o.value===cur))sel.value=cur}await loadControlReviewQueue();renderControl();clearInterval(liveState.timer);liveState.timer=setInterval(renderControl,1000)}catch(e){toast(e.message,true)}}
  function openManualControl(staffId,staffName,action){
    controlTarget={staffId,staffName};
    $('liveControlModalTitle').textContent=`${staffName} · Manual Attendance`;
    $('liveControlAction').value=action||'CHECK_IN';
    const now=new Date(Date.now()+liveState.serverOffset);now.setMinutes(now.getMinutes()-now.getTimezoneOffset());$('liveControlTime').value=now.toISOString().slice(0,16);
    $('liveControlBreak').value='true';$('liveControlNote').value='';
    $('liveControlModal').classList.remove('hidden');
  }
  function closeControlModal(){$('liveControlModal')?.classList.add('hidden');controlTarget=null}
  async function saveManualControl(){
    if(!controlTarget)return;
    const action=$('liveControlAction').value,when=$('liveControlTime').value,note=$('liveControlNote').value.trim();
    if(!when)return toast('Select attendance date and time.',true);
    if(!confirm(`Save manual ${action.replace('_',' ')} for ${controlTarget.staffName}?`))return;
    try{
      const d=new Date(when);if(Number.isNaN(d.getTime()))throw new Error('Invalid date/time.');
      await rpc('attendance_live_admin_manual_action',{p_token:ctx().token,p_staff_id:controlTarget.staffId,p_action:action,p_at:d.toISOString(),p_break_taken:$('liveControlBreak').value==='true',p_note:note});
      toast('Manual attendance saved and audit history updated.');closeControlModal();await loadControl();
    }catch(e){toast(e.message,true)}
  }
  function openManagement(){window.open('goya-live.html','_blank','noopener')}
  function openControl(){
    fillStaffFilter();
    const dateInput=$('liveControlDate');
    if(dateInput&&!dateInput.value)dateInput.value=new Date().toISOString().slice(0,10);
    loadControl();
  }
  function openDashboard(){fillStaffFilter();if(!$('liveDashDate').value)$('liveDashDate').value=new Date().toISOString().slice(0,10);loadDashboard()}
  function openHistory(){fillStaffFilter();const d=new Date(),end=d.toISOString().slice(0,10),month=end.slice(0,7),start=`${month}-01`;if(!$('liveHistMonth').value)$('liveHistMonth').value=month;if(!$('liveHistFrom').value)$('liveHistFrom').value=start;if(!$('liveHistTo').value)$('liveHistTo').value=end;loadHistory()}
  function openSettings(){loadSettings()}
  window.GoyaLiveAdmin={openDashboard,loadDashboard,setDashboardFilter,openControl,loadControl,setControlFilter,renderControl,openHistoryForRecord,openManualControl,closeControlModal,saveManualControl,openHistory,loadHistory,renderHistory,applyMonth,saveClassification,saveClassificationMobile,deleteHistoryDay,editDay,addLegacySecondSession,saveEditBreak,savePreviousRecordEdit,saveSessionEdit,closeEdit,toggleHistoryMark,toggleAllHistory,clearHistoryMarks,bulkSetBreak,bulkEditSelected,exportExcel,exportPDF,exportOvertimeExcel,exportOvertimePDF,openSettings,loadSettings,detectLocation,detectIp,addLocation,removeLocation,saveSettings,openManagement};
  if(window.GoyaAttendance){window.GoyaAttendance.openLiveDashboard=openDashboard;window.GoyaAttendance.openLiveHistory=openHistory;window.GoyaAttendance.openLiveSettings=openSettings;}
})();
